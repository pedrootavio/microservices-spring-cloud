
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pedrootavio/microservice-repo.git
          default-label: main
          search-paths: fornecedor, loja